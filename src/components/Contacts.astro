<section id="contactame" class="bg-gradient-to-b from-sky-50 to-sky-200 py-16">
  <div class="container mx-auto flex flex-col text-center px-6">
    <h1
      class="reveal reveal-up text-sky-900 text-4xl md:text-5xl font-semibold mb-4"
    >
      Contactanos
    </h1>
    <p class="reveal reveal-up reveal-delay-1 text-sky-700 mb-8 lg:text-2xl italic max-w-2xl mx-auto">
      "Contactanos y resolveremos tus dudas con la mejor atencion."
    </p>
    <div class="flex flex-col lg:flex-row justify-center items-center gap-12">
      <h2
        class="reveal reveal-scale text-sky-900 text-3xl md:text-4xl font-bold mb-6"
      >
        +593 95 964 9714
      </h2>
    </div>
  </div>
  <div class="flex flex-col lg:flex-row justify-center items-center gap-12">
    <!-- Llamar directamente -->
    <div
      class="reveal reveal-scale reveal-delay-1 w-full max-w-xs relative rounded-full cursor-pointer transition-all duration-300 delay-150 ease-in-out hover:scale-110 hover:-translate-y-1 p-px overflow-hidden shadow-lg shadow-sky-700/50"
    >
      <!-- Capa del borde animado -->
      <div
        class="absolute inset-[-4px] rounded-full animate-border-rotate bg-[conic-gradient(from_var(--border-angle),#0ea5e9_0%,#38bdf8_50%,#0284c7_100%)]"
      >
      </div>
      <!-- Contenido -->
      <a
        href="tel:+593959649714"
        class="relative bg-sky-900 hover:bg-sky-900/50 rounded-full px-8 py-3 text-white md:text-xl font-bold text-center z-10 block"
      >
        Llamar Ahora
      </a>
    </div>
    <!-- Ir al whatsapp -->
    <div
      class="reveal reveal-scale reveal-delay-2 w-full max-w-xs relative rounded-full cursor-pointer transition-all duration-300 delay-150 ease-in-out hover:scale-110 hover:-translate-y-1 p-px overflow-hidden shadow-lg shadow-sky-700/50"
    >
      <!-- Capa del borde animado -->
      <div
        class="absolute inset-[-4px] rounded-full animate-border-rotate bg-[conic-gradient(from_var(--border-angle),#0ea5e9_0%,#38bdf8_50%,#0284c7_100%)]"
      >
      </div>
      <!-- Contenido -->
      <a
        href="https://wa.me/593959649714"
        target="_blank"
        class="relative bg-sky-900 hover:bg-sky-900/50 rounded-full px-8 py-3 text-white md:text-xl font-bold text-center z-10 block"
      >
        Solicitar por Whatsapp
      </a>
    </div>
  </div>
</section>

<style>
  /* Registro de la propiedad personalizada */
  @property --border-angle {
    syntax: "<angle>";
    inherits: false;
    initial-value: 0deg;
  }

  /* Animacion del borde */
  .animate-border-rotate {
    animation: border-rotate 3s linear infinite;
  }

  @keyframes border-rotate {
    0% {
      --border-angle: 0deg;
    }
    100% {
      --border-angle: 360deg;
    }
  }

  /* Aseguramos que solo se vea el borde */
  .animate-border-rotate {
    -webkit-mask:
      linear-gradient(#fff 0 0) content-box,
      linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    padding: 4px; /* Grosor del borde */
  }
</style>
